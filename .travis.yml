language: python
cache: pip
matrix:
    include:
        # macOS
        - language: generic
          os: osx
          env: PYVER=py27
        - language: generic
          os: osx
          env: PYVER=py36
        # Linux
        - python: 2.7
        - python: 2.7
          arch: arm64
        - python: 3.5
        - python: 3.5
          arch: arm64
        - python: 3.6
        - python: 3.6
          arch: arm64
        - python: 3.7
        - python: 3.7
          arch: arm64
        - python: 3.8
        - python: 3.8
          arch: arm64
        # pypy
        # - python: pypy
        - python: pypy3
install:
    - ./.ci/travis/install.sh
    - ls /var/snap/lxd/common/lxd/storage-pools/instances/containers/travis-job-odidev-psutil-366225515/rootfs/opt/python/3.5.7/lib/python3.5/
script:
    - ./.ci/travis/run.sh
after_success:
    - |
        if [[ $TRAVIS_PYTHON_VERSION == '2.7' ]] && [[ "$(uname -s)" != 'Darwin' ]]; then
            echo "sending test coverage results to coveralls.io"
            coveralls
        fi
